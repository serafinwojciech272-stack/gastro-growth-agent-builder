import { FormEvent, useEffect, useState } from 'react';
import { ArrowLeft, CheckCircle2, Loader2, Sparkles, Utensils } from 'lucide-react';
import { Link, useNavigate } from 'react-router-dom';
import { useAuth } from '../context/AuthContext';
import { requireSupabase } from '../lib/supabase';

type MenuItem={name:string;category:string;description:string;price:number|null};
type Recommendation={title:string;rationale:string;priority:string;actions:string[]};
type Result={analysis_id:string;score:number;summary:string;strengths:string[];issues:string[];opportunities:string[];recommendations:Recommendation[]};

const example='STARTERS\nBruschetta al Pomodoro | 28 | Toasted sourdough, tomato, basil, extra virgin olive oil\nBurrata | 39 | Creamy burrata, roasted tomatoes, basil oil\n\nMAINS\nTagliatelle Bolognese | 49 | Fresh pasta, beef and pork ragu, parmesan\nGrilled Sea Bass | 72 | Seasonal vegetables, lemon butter\n\nDESSERTS\nTiramisu | 29 | Mascarpone, espresso, cocoa';

function parseMenu(text:string):MenuItem[]{
 const items:MenuItem[]=[]; let category='Other';
 for(const line of text.split(/\r?\n/)){const s=line.trim();if(!s)continue;if(!s.includes('|')){category=s.replace(/[:]+$/,'').trim();continue}const p=s.split('|').map(x=>x.trim());const price=Number(p[1]);items.push({category,name:p[0],price:Number.isFinite(price)?price:null,description:p.slice(2).join(' | ')})}return items;
}

export default function MenuPage(){
 const {user,loading}=useAuth();const navigate=useNavigate();const [menu,setMenu]=useState(example);const [busy,setBusy]=useState(false);const [error,setError]=useState('');const [result,setResult]=useState<Result|null>(null);
 useEffect(()=>{if(!loading&&!user)navigate('/login',{replace:true})},[loading,user,navigate]);
 async function submit(e:FormEvent){e.preventDefault();const items=parseMenu(menu);if(items.length<2){setError('Add at least two menu items. Use: Item | price | description');return}setBusy(true);setError('');setResult(null);try{const supabase=requireSupabase();const {data:members,error:me}=await supabase.from('organization_members').select('organization_id').eq('user_id',user!.id).limit(1);if(me)throw me;const org=members?.[0]?.organization_id;if(!org)throw new Error('No restaurant workspace found. Complete onboarding first.');const {data:restaurant,error:re}=await supabase.from('restaurants').select('id').eq('organization_id',org).limit(1).single();if(re)throw re;const {data:m,error:mi}=await supabase.from('menus').insert({restaurant_id:restaurant.id,name:'Main Menu',source_type:'manual',raw_text:menu,status:'analyzing'}).select('id').single();if(mi)throw mi;const {error:ii}=await supabase.from('menu_items').insert(items.map((x,i)=>({menu_id:m.id,category:x.category,name:x.name,description:x.description,price:x.price,position:i})));if(ii)throw ii;const {data:ai,error:ae}=await supabase.functions.invoke('gga-menu-analyzer',{body:{menu_id:m.id}});if(ae)throw ae;if(ai?.error)throw new Error(ai.error);setResult(ai)}catch(x){setError(x instanceof Error?x.message:'Menu analysis failed.')}finally{setBusy(false)}}
 if(loading||!user)return <div className="min-h-screen bg-[#080809] text-white grid place-items-center text-sm text-[#a1a1aa]">Loading...</div>;
 return <main className="min-h-screen bg-[#080809] text-[#f5f5f5] px-5 py-8 lg:px-8"><div className="mx-auto max-w-6xl"><div className="mb-10 flex items-center justify-between"><Link to="/app/dashboard" className="inline-flex items-center gap-2 text-sm text-[#a1a1aa] hover:text-white"><ArrowLeft size={16}/> Dashboard</Link><span className="inline-flex items-center gap-2 text-xs text-[#71717a]"><Utensils size={15} className="text-[#a78bfa]"/> Menu Intelligence</span></div><section><p className="text-xs uppercase tracking-[0.18em] text-[#a78bfa]">AI menu engineering</p><h1 className="mt-2 text-4xl font-semibold tracking-tight">Turn your menu into a growth asset.</h1><p className="mt-3 max-w-2xl text-sm leading-6 text-[#a1a1aa]">Paste your menu. GGA stores the structure, analyzes pricing and descriptions, then returns prioritized improvements.</p></section><form onSubmit={submit} className="mt-8 grid gap-5 lg:grid-cols-[1fr_360px]"><section className="rounded-2xl border border-[#27272a] bg-[#111113] p-5"><label className="text-sm font-medium">Menu data</label><p className="mt-1 text-xs text-[#71717a]">Format: Item | price | description. Put category names on separate lines.</p><textarea value={menu} onChange={e=>setMenu(e.target.value)} rows={18} className="mt-4 w-full rounded-xl border border-[#27272a] bg-[#09090b] p-4 font-mono text-sm leading-6 outline-none focus:border-[#a78bfa]"/><div className="mt-4 flex items-center justify-between gap-4"><span className="text-xs text-[#52525b]">{parseMenu(menu).length} items detected</span><button disabled={busy} className="inline-flex items-center gap-2 rounded-lg bg-[#f5f5f5] px-5 py-3 text-sm font-semibold text-[#09090b] disabled:opacity-50">{busy?<Loader2 size={16} className="animate-spin"/>:<Sparkles size={16}/>} {busy?'Analyzing...':'Analyze menu'}</button></div>{error&&<p role="alert" className="mt-4 rounded-lg border border-red-500/30 bg-red-500/10 p-3 text-sm text-red-200">{error}</p>}</section><aside className="rounded-2xl border border-[#27272a] bg-[#111113] p-5"><p className="text-xs uppercase tracking-wider text-[#71717a]">Analysis scope</p><div className="mt-4 space-y-3 text-sm text-[#d4d4d8]"><div>Pricing and price architecture</div><div>Category structure</div><div>Descriptions and perceived value</div><div>Upsell and cross-sell opportunities</div><div>Weak menu items and priorities</div></div></aside></form>{result&&<div className="mt-8 space-y-5"><section className="rounded-2xl border border-[#27272a] bg-[#111113] p-6"><div className="flex flex-wrap items-center justify-between gap-4"><div><p className="text-xs uppercase tracking-wider text-[#71717a]">Menu health</p><p className="mt-2 text-5xl font-semibold">{result.score}<span className="text-xl text-[#71717a]">/100</span></p></div><div className="max-w-2xl text-sm leading-7 text-[#d4d4d8]">{result.summary}</div></div></section><div className="grid gap-5 md:grid-cols-3">{[['Strengths',result.strengths],['Issues',result.issues],['Opportunities',result.opportunities]].map(([title,list])=><section key={title as string} className="rounded-2xl border border-[#27272a] bg-[#111113] p-5"><h2 className="font-semibold">{title as string}</h2><div className="mt-4 space-y-3">{(list as string[]).map(x=><div key={x} className="text-sm leading-6 text-[#a1a1aa]">{x}</div>)}</div></section>)}</div><section><h2 className="mb-4 text-xl font-semibold">Priority recommendations</h2><div className="grid gap-5 md:grid-cols-2">{result.recommendations.map(r=><article key={r.title} className="rounded-2xl border border-[#27272a] bg-[#111113] p-5"><div className="flex justify-between gap-3"><h3 className="font-semibold">{r.title}</h3><span className="text-xs uppercase text-[#c4b5fd]">{r.priority}</span></div><p className="mt-3 text-sm leading-6 text-[#a1a1aa]">{r.rationale}</p><div className="mt-4 space-y-2">{r.actions.map(a=><div key={a} className="flex gap-2 text-sm text-[#d4d4d8]"><CheckCircle2 size={15} className="mt-0.5 shrink-0 text-[#a78bfa]"/>{a}</div>)}</div></article>)}</div></section></div>}</div></main>;
}
