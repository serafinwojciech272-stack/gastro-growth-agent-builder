import { FormEvent, useEffect, useState } from 'react';
import { ArrowLeft, ArrowRight, Bot, CheckCircle2, ChevronRight, Loader2, Sparkles, Target } from 'lucide-react';
import { Link, useNavigate } from 'react-router-dom';
import { useAuth } from '../context/AuthContext';
import { requireSupabase } from '../lib/supabase';

type Recommendation={title:string;rationale:string;priority:string;actions:string[]};
type Result={analysis_id:string;restaurant:{name:string};diagnosis:string;root_causes:string[];recommendations:Recommendation[];priority:string};
export default function AdvisorPage(){
 const {user,loading}=useAuth(); const navigate=useNavigate(); const [problem,setProblem]=useState(''); const [busy,setBusy]=useState(false); const [error,setError]=useState(''); const [result,setResult]=useState<Result|null>(null);
 useEffect(()=>{if(!loading&&!user)navigate('/login',{replace:true})},[loading,user,navigate]);
 async function submit(e:FormEvent){e.preventDefault();if(problem.trim().length<8){setError('Describe the business problem in at least 8 characters.');return}setBusy(true);setError('');setResult(null);try{const {data,error}=await requireSupabase().functions.invoke('gga-advisor',{body:{problem:problem.trim()}});if(error){const context=(error as any)?.context;let detail='';if(context?.json){try{const body=await context.json();detail=body?.error?String(body.error):''}catch{/* Ignore malformed error context. */}}else if(context?.body){try{const raw=typeof context.body==='string'?context.body:JSON.stringify(context.body);const body=JSON.parse(raw);detail=body?.error?String(body.error):''}catch{/* Ignore malformed error context. */}}throw new Error(detail||error.message||'GA Advisor request failed.')}if(data?.error)throw new Error(String(data.error));setResult(data)}catch(x){setError(x instanceof Error?x.message:'GA analysis failed.')}finally{setBusy(false)}}
 if(loading||!user)return <div className="min-h-screen bg-[#07080c] text-white grid place-items-center text-sm text-slate-400">Loading...</div>;
 return <main className="min-h-screen bg-[#07080c] text-white px-5 py-8 lg:px-8"><div className="mx-auto max-w-6xl"><div className="mb-10 flex items-center justify-between"><Link to="/app/dashboard" className="inline-flex items-center gap-2 text-sm text-slate-400 hover:text-white"><ArrowLeft size={16}/> Command Center</Link><div className="flex items-center gap-3"><Link to="/app/missions" className="hidden items-center gap-1 text-xs text-slate-500 hover:text-white sm:inline-flex">Mission Control <ArrowRight size={13}/></Link><span className="inline-flex items-center gap-2 text-xs text-slate-400"><Bot size={15} className="text-violet-300"/> GA / Growth Advisor</span></div></div><section className="max-w-3xl"><p className="text-xs uppercase tracking-[0.18em] text-violet-300">Business intelligence</p><h1 className="mt-2 text-4xl font-semibold tracking-tight">What is the biggest growth problem in your business right now?</h1><p className="mt-3 text-sm leading-6 text-slate-400">GA uses your business context as evidence, separates facts from hypotheses, diagnoses likely constraints, and turns the strongest opportunity into prioritized actions.</p></section><form onSubmit={submit} className="mt-8 rounded-2xl border border-white/[.08] bg-[#0d0f15] p-5 shadow-2xl sm:p-6"><label className="block text-sm font-medium">Describe the problem</label><textarea value={problem} onChange={e=>setProblem(e.target.value)} maxLength={4000} rows={6} placeholder="Example: Revenue is flat, acquisition costs are rising, and repeat customers are declining." className="mt-3 w-full resize-y rounded-xl border border-white/[.08] bg-[#080a0f] px-4 py-4 text-sm leading-6 outline-none placeholder:text-slate-600 focus:border-violet-400/60"/><div className="mt-3 flex flex-col gap-3 sm:flex-row sm:items-center sm:justify-between"><span className="text-xs text-slate-600">Include symptom, timing, business impact and any evidence you already have.</span><button disabled={busy} className="inline-flex min-h-11 items-center justify-center gap-2 rounded-lg bg-white px-5 py-3 text-sm font-semibold text-slate-950 disabled:opacity-50">{busy?<Loader2 size={16} className="animate-spin"/>:<Sparkles size={16}/>} {busy?'Analyzing...':'Analyze with GA'} <ChevronRight size={16}/></button></div>{error&&<p role="alert" className="mt-4 rounded-lg border border-red-500/30 bg-red-500/10 p-3 text-sm text-red-200">{error}</p>}</form>{result&&<div className="mt-8 space-y-5"><section className="rounded-2xl border border-white/[.08] bg-[#0d0f15] p-6"><div className="flex flex-wrap items-center justify-between gap-3"><div><p className="text-xs uppercase tracking-[0.18em] text-slate-500">Diagnosis</p><h2 className="mt-2 text-xl font-semibold">{result.restaurant.name}</h2></div><span className="rounded-full border border-violet-400/20 px-3 py-1 text-xs uppercase text-violet-200">{result.priority} priority</span></div><p className="mt-5 max-w-3xl text-sm leading-7 text-slate-300">{result.diagnosis}</p>{result.root_causes.length>0&&<div className="mt-6"><p className="text-xs uppercase tracking-wider text-slate-500">Root causes / hypotheses</p><div className="mt-3 grid gap-2 sm:grid-cols-2">{result.root_causes.map(c=><div key={c} className="rounded-lg border border-white/[.07] bg-[#090b10] p-3 text-sm text-slate-300">{c}</div>)}</div></div>}</section><div className="grid gap-5 lg:grid-cols-2">{result.recommendations.map(r=><section key={r.title} className="rounded-2xl border border-white/[.08] bg-[#0d0f15] p-6"><div className="flex items-start justify-between gap-3"><h3 className="font-semibold">{r.title}</h3><span className="text-xs uppercase text-violet-300">{r.priority}</span></div><p className="mt-3 text-sm leading-6 text-slate-400">{r.rationale}</p><div className="mt-5 space-y-2">{r.actions.map(a=><div key={a} className="flex gap-2 text-sm text-slate-300"><CheckCircle2 size={16} className="mt-0.5 shrink-0 text-violet-300"/>{a}</div>)}</div></section>)}</div><div className="flex flex-col gap-3 rounded-2xl border border-orange-400/20 bg-orange-400/[.04] p-5 sm:flex-row sm:items-center sm:justify-between"><div><div className="flex items-center gap-2 text-sm font-semibold text-white"><Target size={16} className="text-orange-300"/> Next: governed execution</div><p className="mt-1 text-xs text-slate-500">Review available mission runs and approval-gated execution in Mission Control.</p></div><Link to="/app/missions" className="inline-flex items-center justify-center gap-2 rounded-xl bg-white px-4 py-2.5 text-sm font-bold text-black">Open Mission Control <ArrowRight size={15}/></Link></div></div>}</div></main>;
}
